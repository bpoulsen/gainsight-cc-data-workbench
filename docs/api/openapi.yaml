openapi: 3.0.0
info:
  title: Customer Communities API Overview
  version: "1.0"
  description:
    $ref: md/info.md
  contact:
    name: Customer Communities support
    url: https://www.gainsight.com/customer-communities/
paths: []
components:
  securitySchemes:
    insided_auth:
      type: oauth2
      flows:
        clientCredentials:
          tokenUrl: /oauth2/token
          scopes:
            read: Read access to the Community API
            write: Write access to the Community API
